---
phase: 999.1
slug: set-spi-display-gpio-pins-to-input-before-deep-sleep
status: partial
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-28
updated: 2026-07-22
---

# Phase 999.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Arduino/ESP-IDF firmware — no automated test framework |
| **Config file** | none — hardware-only verification |
| **Quick run command** | `pio run` (compile check) |
| **Full suite command** | Flash + measure sleep current with multimeter |
| **Estimated runtime** | ~5 min (compile + flash + measure) |

---

## Sampling Rate

- **After every task commit:** Run `pio run` to verify it compiles
- **After every plan wave:** Flash device and measure deep sleep current
- **Before `/gsd:verify-work`:** Before/after current measurement documented
- **Max feedback latency:** 300 seconds (5 min compile + flash + measure)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 999.1-01-01 | 01 | 1 | SLEEP-01 | manual | `pio run` (compile) | ✅ | ✅ green |
| 999.1-01-02 | 01 | 1 | SLEEP-01 | manual | `pio run` (compile) | ✅ | ✅ green |
| 999.1-01-03 | 01 | 1 | SLEEP-02, SLEEP-03 | manual | flash + multimeter | ✅ | ✅ green |
| 999.1-addendum-01 | — (no plan; commit `9f3dd7f`) | — | SLEEP-04 | manual | `pio run` (compile) | ✅ | ✅ green |
| 999.1-addendum-02 | — (no plan; commit `9f3dd7f`) | — | SLEEP-04 | manual | flash + trigger low-battery guard path | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Note on 999.1-addendum rows:** the `enforceLowBatteryGuard()` GPIO tri-state cleanup (commit `9f3dd7f`) replicates the `hibernate()` pattern verified under SLEEP-01..03, but was implemented without a plan file after this phase was already marked complete — it was originally logged as an out-of-scope "Follow-up Candidate" in `999.1-01-SUMMARY.md`. SLEEP-04 was retroactively added to `REQUIREMENTS.md` on 2026-07-22 to track it. The compile check is satisfiable from the existing code (`grep` confirms correct line ordering), but the hardware checkpoint (flash + trigger the 24h critical-low-battery path + confirm wake/render) has **not** been performed for this specific code path and remains outstanding before merge.

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements (Arduino/ESP-IDF firmware — no test framework needed).*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Deep sleep current reduced | SLEEP-03 | Requires hardware measurement | Measure µA with multimeter on VCC rail during deep sleep before and after change |
| Device still wakes correctly | SLEEP-02 | Requires live device | Flash, trigger deep sleep, verify wakeup on next timer/RTC trigger |
| SPI re-initializes after wakeup | SLEEP-02 | Requires full cycle test | Flash, deep sleep, wake, verify image update succeeds |
| enforceLowBatteryGuard() still compiles and the 24h critical-low-battery path still enters/wakes from deep sleep correctly after the tri-state cleanup | SLEEP-04 | Requires live device on the low-battery guard path specifically — not exercised by the SLEEP-01..03 hardware checkpoint, which only ran the `hibernate()` path | Flash, force battery voltage into the low-battery guard threshold (or stub the check), confirm the device still enters deep sleep and wakes/re-renders on the next timer trigger — **not yet performed** |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 300s
- [ ] `nyquist_compliant: true` set in frontmatter — blocked on SLEEP-04 hardware checkpoint

**Approval:** partial — SLEEP-01, SLEEP-02, SLEEP-03 approved 2026-06-28 (see `999.1-VERIFICATION.md`). SLEEP-04 added 2026-07-22, compile-verified, hardware checkpoint outstanding.

---

## Validation Audit 2026-07-22

| Metric | Count |
|--------|-------|
| Gaps found | 1 (SLEEP-04 — undocumented `enforceLowBatteryGuard()` commit with no requirement, no plan, no hardware checkpoint) |
| Resolved | 0 (no automated test possible — Arduino firmware; retroactively added SLEEP-04 requirement + compile-check verification) |
| Escalated | 1 (SLEEP-04 hardware checkpoint — needs an operator to flash and exercise the low-battery guard path before this can be marked `nyquist_compliant: true`) |

**Context:** this audit was triggered by a PR review of `feature/battery-optimization` (Phase 10 + Phase 999.1). SLEEP-01..03 were already correctly verified on 2026-06-28 but this file's status/task rows were never updated to reflect that. `enforceLowBatteryGuard()` (commit `9f3dd7f`) replicates the SLEEP-01..03 pattern but was added after phase completion, without going through `/gsd:plan-phase` or `/gsd:execute-phase` — it has no plan or SUMMARY of its own. Recommend a `/gsd:quick` task to run the missing hardware checkpoint and update this file's SLEEP-04 status before merging.
